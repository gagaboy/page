define(['../Base'], function (Base) {
    var BaseLayout = new Class({
        Extends: Base,
        initialize: function () {

        }
    });
    BaseLayout.xtype = "baseLayout";
    return BaseLayout;
});